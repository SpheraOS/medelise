'use client';

export default function LoginPage() {
    return (
        <main
            style={{
                minHeight: '100vh',
                background: 'var(--color-surface, #f5f5f5)',
            }}
        >
            {/* Login page — Navbar is rendered by the root layout */}
        </main>
    );
}
